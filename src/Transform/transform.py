"""Passo 4 — Transformações e criação de novas colunas (STUB).

A implementação completa entra no próximo passo: colunas de data/hora/turno,
velocidade, tip_pct, faixas de distância/duração, rentabilidade e flags.
"""

from __future__ import annotations

import pandas as pd


def transform(df: pd.DataFrame) -> pd.DataFrame:
    """Placeholder: retorna o DataFrame inalterado."""
    return df
